	{{!--
	<p> Hello World......</p>
	{{#each data}}
	<h2>{{this.title}}</h2>

	<strong>{{this.teaser}}</strong>

	<p>{{this.link}}</p>

	<h3 data-id='{{id}}'>{{headline}}</h3>
	<a href={{link}}>Get Showtimes</a>
	<br> {{summary}}
	<br>
	<button data-id='{{_id}}' id='savemovie'>Save Movie</button>

	console.log({{_id}})
	<br>
	</div> --}}

	<div class="jumbotron jumbotron-fluid">
		<div class="container">
			<h1 class="display-3">
				<span class="glyphicon glyphicon-search" style="font-size: 40px;"></span> Saved Articles</h1>
			<p class="lead">Web Scraper built using: Node.js, Cheerio, Express, Mongoose & Handlebars</p>
			<button type="button" class="btn btn-warning" id="home">HOME</button>



		</div>
	</div>



{{#each data}}
<div class="container">
		<div class="card">
			<h5 class="card-header">{{this.title}}</h5>
			<div class="card-body">
				{{!-- <h5 class="card-title">{{this.date}}</h5> --}}
				<p class="card-text">{{this.teaser}}</p>
				<h3 data-id='{{id}}'>{{headline}}</h3>
				<a href={{this.link}} class="btn btn-primary">Read More</a>
				{{!-- <button type="button" class="btn btn-success saveArticle" data-id='{{_id}}'>Save Article</button> --}}



				{{#if this.saved}}
					<button type="button" class="btn btn-success saveArticle" disabled data-id='{{_id}}'>Save Article</button>

				{{else}}
					<button type="button" class="btn btn-success saveArticle" data-id='{{_id}}'>Save Article</button>

				{{/if}}

				<button type="button" class="btn btn-danger delArticle" data-id='{{_id}}'>Delete Article</button>

				{{!-- <p>
					<a class="btn btn-primary" data-toggle="collapse" href="#collapseExample" role="button" aria-expanded="false" aria-controls="collapseExample">
						Link with href
					</a> --}}
					{{!-- <button class="btn btn-primary float-right" type="button" data-toggle="collapse" data-id='{{_id}}' data-target='{{concat("collapseExample" _id)}}' aria-expanded="false" aria-controls="collapseExample"> --}}
					<button class="btn btn-primary float-right" type="button" data-toggle="collapse" data-id='{{_id}}' data-target="collapseExample" aria-expanded="false" aria-controls="collapseExample">

						Add Note
					</button>
				{{!-- </p> --}}
				<p></p>
				{{!-- <div class="collapse" id='{{concat("collapseExample" _id)}}'> --}}
				{{!-- <div class="collapse" id="collapseExample" + '{{_id}}' >

					<div class="card card-body">
						Anim pariatur cliche reprehenderit, enim eiusmod high life accusamus terry richardson ad squid. Nihil anim keffiyeh helvetica,
						craft beer labore wes anderson cred nesciunt sapiente ea proident.
					</div>
				</div> --}}

				{{!--
				<p>
					<a class="btn btn-primary" data-toggle="collapse" href="#collapseExample" role="button" aria-expanded="false" aria-controls="collapseExample">
						Link with href
					</a>
					<button class="btn btn-primary" type="button" data-toggle="collapse" data-target="#collapseExample" aria-expanded="false"
					 aria-controls="collapseExample">
						Button with data-target
					</button>
				</p>
				<div class="collapse" id="collapseExample">
					<div class="card card-body">
						Anim pariatur cliche reprehenderit, enim eiusmod high life accusamus terry richardson ad squid. Nihil anim keffiyeh helvetica,
						craft beer labore wes anderson cred nesciunt sapiente ea proident.
					</div>
				</div> --}}


			</div>
		</div>
	</div>
	{{/each}} {{!--
	<script>
		$("button").on("click", function () {
			console.log("Hello")
		})
});

		$.ajax({
			url: "/articles/" + ,
			data: data,
			success: success,
			dataType: dataType
		});
	</script> --}} {{!--
	<script>
		var clkButton = $('.btn-success');
		clkButton.click(function (e) {
			const id = $(this).attr('data-id');
			console.log(id)
			$.ajax({
				url: "/articles/" + id,
				method: "GET"
			}).then(function (data) {
				window.location.reload();
			})
		})--}}

	</script>
	<script src="/js/app.js"></script>


</html>





